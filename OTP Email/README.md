## OTP Email Solution

This is a simple One Time Password (OTP) solution integrated through a simple SMTP server.

This works by sending a OTP through to a selected email address and before a certain time if that OTP is entered it will authenticate the user.
Otherwise the code becomes invalid and a new OTP would have to be sent out.
