## Discretionary Access Control Design

This is a Discretrioanry Access Control design based off the permissions granted for admins and guest users when interacting with a database.

The assignment of these permissions is done by the owner (The person managing the script / system).

Techonogies Used:
- Python
- SQL / SQLite3

